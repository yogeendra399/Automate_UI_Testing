#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@lambdatest/smartui-cli/dist/index.cjs" "$@"
else 
  exec node  "$basedir/../@lambdatest/smartui-cli/dist/index.cjs" "$@"
fi
